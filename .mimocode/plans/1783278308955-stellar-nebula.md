# Plano: Revisão Completa do Site Desysis

## Contexto
Site estático HTML/CSS/JS para portfólio da marca "Desysis — Design Systems". Precisa de correções de bugs, melhorias de design, funcionalidades novas e refatoração. Mantido como site estático (sem build tools).

---

## 1. Corrigir Bugs

### 1.1 Email duplicado no href
- **Arquivo**: `index.html:223`
- **Bug**: `mailto:desysis00@gmail.com.com` → corrigir para `mailto:desysis00@gmail.com`

### 1.2 Stats counter sem HTML correspondente
- **Arquivo**: `script.js:74-96`
- **Bug**: JS observa `.stat-number` mas não existem no HTML
- **Ação**: Remover o bloco de stats observer do JS

### 1.3 LinkedIn comentado
- **Arquivo**: `index.html:236-243`
- **Ação**: Remover permanentemente (comentário + código morto)

---

## 2. Melhorias de Design e Conteúdo

### 2.1 SEO e Meta tags
- Adicionar `<meta name="description">`
- Adicionar Open Graph tags (og:title, og:description, og:image)
- Adicionar favicon (usar logo existente)

### 2.2 Melhorar seção Portfólio
- Adicionar link "Ver projeto" nos cards de portfolio
- Melhorar hover com overlay mais elaborado

### 2.3 Footer dinâmico
- Ano dinâmico via JS (`new Date().getFullYear()`)

---

## 3. Funcionalidades Novas

### 3.1 Formulário de contato funcional
- Reativar o formulário HTML (remover comentários)
- Validação client-side (campos obrigatórios, email válido)
- Feedback visual: botão muda para "Enviado!" com animação
- Nota: sem backend, apenas feedback visual (o form já tinha isso no JS original)

### 3.2 Voltar ao topo
- Botão flutuante "scroll to top" que aparece após 300px de scroll
- Animação suave de fade-in/out

### 3.3 Performance
- Adicionar `loading="lazy"` nas imagens do portfolio e hero
- Adicionar `<link rel="preconnect">` para Google Fonts (já existe)

---

## 4. Refatoração

### 4.1 Separar CSS em módulos
Manter um único `style.css` mas organizar com seções claras:
```
style.css (organizado):
  /* === Reset & Variables === */
  /* === Base & Typography === */
  /* === Layout === */
  /* === Header & Navigation === */
  /* === Hero === */
  /* === About === */
  /* === Services === */
  /* === Portfolio === */
  /* === Contact === */
  /* === Footer === */
  /* === Animations === */
  /* === Responsive === */
```

### 4.2 Organizar JavaScript
Manter um único `script.js` mas separar em funções claras:
```
script.js (organizado):
  /* === Navigation === */
  /* === Smooth Scroll === */
  /* === Active Section === */
  /* === Animations === */
  /* === Form === */
  /* === Scroll to Top === */
  /* === Init === */
```

---

## 5. Arquivos a modificar

| Arquivo | Ações |
|---------|-------|
| `index.html` | Corrigir email, reativar form, adicionar meta tags, lazy loading, botão scroll-to-top |
| `style.css` | Reorganizar seções, adicionar estilos do botão scroll-to-top |
| `script.js` | Remover stats observer, adicionar scroll-to-top, ano dinâmico, organizar código |

---

## 6. Verificação

1. Abrir `index.html` no navegador
2. Testar todas as seções e navegação
3. Testar responsividade (mobile, tablet, desktop)
4. Testar formulário de contato
5. Testar animações de scroll
6. Verificar que não há erros no console
7. Testar botão "voltar ao topo"
